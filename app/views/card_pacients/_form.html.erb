<%= form_with(model: card_pacient) do |form| %>
  <% if card_pacient.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(card_pacient.errors.count, "error") %> prohibited this card_pacient from being saved:</h2>

      <ul>
        <% card_pacient.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
