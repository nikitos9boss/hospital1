<%= form_with(model: special) do |form| %>
  <% if special.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(special.errors.count, "error") %> prohibited this special from being saved:</h2>

      <ul>
        <% special.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
